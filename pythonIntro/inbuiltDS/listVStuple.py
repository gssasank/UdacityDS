tuple = () # ordered and immutable, parantheses are optional while making tuples

list = []

dimensions = 52, 40, 100
length, width, height = dimensions # tuple unpacking
print("The dimensions are {} x {} x {}".format(length, width, height)) # string formatting is very useful